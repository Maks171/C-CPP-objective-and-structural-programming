#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int x=12;
    if(x%2==0) {
        printf("Liczba parzysta");
    }
    else {
        printf("Liczba nieparzysta");
    }
    return EXIT_SUCCESS;
}
